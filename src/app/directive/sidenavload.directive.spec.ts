import { SidenavloadDirective } from './sidenavload.directive';

describe('SidenavloadDirective', () => {
  it('should create an instance', () => {
    const directive = new SidenavloadDirective();
    expect(directive).toBeTruthy();
  });
});
